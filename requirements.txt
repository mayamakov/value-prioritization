# Python dependencies for reproducing the analyses in
# "LLMs and Physicians Prioritize Preventive-Care Patients by Different Values"
#
# Tested with Python 3.11 and 3.12.
# Install with:  pip install -r requirements.txt
#
# The pins below are lower bounds known to work. The pipeline avoids
# version-sensitive APIs; newer releases within the same major line are fine.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
torch>=2.0            # CPU build is sufficient; GPU only speeds up the optional SHAP step
matplotlib>=3.7
seaborn>=0.12
shap>=0.44           # only needed for --with-shap (recomputing attributions on GPU)
xgboost>=2.0
lightgbm>=4.0
openpyxl>=3.1        # reads the synthetic-cohort .xlsx
graphviz>=0.20       # only for the study-design schematic (Figure 1)
